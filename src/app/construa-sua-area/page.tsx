import { Section } from "@/components/Section";
import { getRoleTemplate } from "@/lib/content/load";
export default function Page(){ const t=getRoleTemplate(); return <><section className="pageHero"><div className="container"><div className="eyebrow">Construção pelos líderes</div><h1>Área → Capabilities → Papéis → Pessoas</h1><p>A macroestrutura não fecha a organização. Ela dá aos Heads um contorno claro para que construam, com autonomia, os papéis necessários para cumprir a missão da área.</p></div></section>
<Section title="Quatro passos"><div className="steps">{[["1","Missão e accountabilities","Entenda o resultado pelo qual sua macroárea é accountable."],["2","Capabilities","Liste as capacidades que precisam existir — sem confundi-las com cargos."],["3","Papéis","Agrupe responsabilidades, autoridade e resultados em Role Charts claros."],["4","Pessoas","Só então atribua uma ou mais pessoas a cada papel."]].map(x=><div className="step" key={x[0]}><span>{x[0]}</span><div><strong>{x[1]}</strong><p>{x[2]}</p></div></div>)}</div></Section>
<Section eyebrow="Princípios" title="Regras do sistema"><div className="grid3"><div className="card"><h3>Pessoa ≠ papel</h3><p>Uma pessoa pode exercer mais de um papel.</p></div><div className="card"><h3>Papel ≠ cargo</h3><p>Nem toda capability exige uma posição dedicada.</p></div><div className="card"><h3>Accountability exige autoridade</h3><p>O Role Chart deve deixar explícito o que o dono do papel pode decidir.</p></div></div></Section>
<Section eyebrow="Template" title="Estrutura mínima de um Role Chart"><pre className="yamlPreview">{Object.entries(t).map(([k,v])=>`${k}: ${Array.isArray(v)?"[...]":typeof v==="object"?"{...}":String(v??"")}`).join("\n")}</pre><p className="sectionIntro">O arquivo-base completo está em <code>content/role-template.yaml</code>. Copie-o, renomeie o slug e preencha os campos.</p></Section>
</> }
