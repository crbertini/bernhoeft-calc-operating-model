import Link from "next/link";
import { getAllRoles } from "@/lib/content/load";
export default function Page(){ const roles=getAllRoles(); return <><section className="pageHero"><div className="container"><div className="eyebrow">Role Charts</div><h1>Papéis claros. Pessoas com autonomia.</h1><p>Os papéis institucionais abaixo definem accountability e autoridade. Novos papéis podem ser criados pelos Heads conforme as capabilities necessárias em cada área.</p></div></section><section className="section"><div className="container"><div className="roleGrid">{roles.map(r=><Link href={`/papeis/${r.slug}`} className={`roleCard ${r.slug==="lucas"?"mentor":""}`} key={r.slug}><span>{r.status||"papel"}</span><h3>{r.title}</h3><p>{r.mission}</p>{r.currentOwner&&<strong>{r.currentOwner}</strong>}</Link>)}</div></div></section></> }
